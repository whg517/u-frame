# 变更记录

本文件记录面向用户和维护者的重要变化。版本采用 SemVer；dev 为开发预发行，非稳定版本。

## 未发布

暂无新的未发布变更。

## 0.1.0-dev.1 — 2026-09-09

本次开发候选通过 GitHub Release 提供 macOS arm64 DMG、Windows amd64 EXE 和 Linux amd64/arm64 DEB。macOS 仅 ad-hoc 签名、无 Apple 公证，Windows 无证书签名；可能出现系统安全提示。Linux 以 Ubuntu 24.04 为构建基线。自动构建不代表四平台人工安装和交互已经验收。

### 新增

- 机房、区域、机柜和四类物理设备资产的本地管理。
- 多机柜 U 位画布、位置筛选、缩放、机柜排序和设备详情。
- 设备首次上架、移动、下架以及画布批量位置调整。
- SQLite 持久化、领域校验、类型安全 Tauri IPC 和完整本地质量门禁。
- GitHub Issue/PR 模板、Dependabot、CI 和签名公证发布工作流。
- 系统/浅色/深色外观、五种主题色、简体中文/英语全局界面及本地偏好持久化。
- 80%、100%、120% 和 140% 默认机柜画布缩放，并支持画布一键回到所选默认值。
- 紧凑、标准、宽松三档界面密度和小、标准、大三档界面字号。
- 机柜一览、位置管理、机柜管理和设备资产四种默认启动页面。

### 优化与修复

- 修复断网时本地查询/保存等待、编辑输入被后台刷新覆盖、无效 IP 校验和偏好存储拒绝访问的问题。
- 补充本地化业务错误、页面错误与未知路径的返回入口，不直接展示内部异常。
- 按页面分包加载，集中跨领域查询并通过 ESLint 检查依赖方向；Rust 应用层按用例拆分并修复特殊字符数据库路径解析。

### 范围调整

- 明确不提供数据备份、跨安装或跨目录迁移、数据恢复及自定义业务数据库位置。
- 隔离纯领域规则与 IPC/SQL，批量调整通过应用层事务端口执行；新增部分写入失败的完整回滚测试。
- 增加根文件与工作流政策、脚本反例、独立 Domain 编译和配置 TypeScript 检查，强化需求双向追踪及四处版本一致性。
- 分离发布来源验证与签名 job，收紧凭据注入范围；发布校验拒绝歧义产物并验证双架构、签名、公证与摘要。

### 发布方式调整

- 上述原 Universal 签名公证方案由四目标原生构建替代，不使用 Apple 发行服务或证书凭据。
- 四目标全部成功且版本、源码、架构和 SHA-256 校验通过后，dev.N 才自动发布为 Pre-release；其余 tag 只创建 Draft。
- Windows/Linux 开启必要桌面权限，修正 Windows 文件路径测试，保留本地数据与最小权限边界。
